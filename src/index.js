import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";




ReactDOM.render(
  <App/>,
  document.getElementById("root")
);

//The code at this point looks really untidy and too much
//to go through as well as repetitive
//we will move this to App.jsx